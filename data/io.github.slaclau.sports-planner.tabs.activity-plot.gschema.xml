<?xml version="1.0" encoding="utf-8"?>
<schemalist>
  <schema id="io.github.slaclau.sports-planner.tabs.activity-plot">
    <key name="columns" type="as">
      <summary>Columns to plot.</summary>
      <description>
        Columns to plot. Non-existent columns are silently ignored.
      </description>
      <default>["speed", "power", "cadence", "heartrate"]</default>
    </key>
  </schema>
</schemalist>
